manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: badjeff
      url-base: https://github.com/badjeff
    - name: pite1222
      url-base: https://github.com/pite1222
    - name: 4mplelab
      url-base: https://github.com/4mplelab
  projects:
    - name: zmk
      remote: pite1222
      revision: feat/pointing-studio
      import: app/west.yml
    - name: zmk-studio-messages
      remote: pite1222
      revision: feat/pointing
      path: modules/msgs/zmk-studio-messages
    - name: zmk-pmw3610-driver
      remote: badjeff
      # TEST: pin to a1a0f35 (2025-12-31, "fix pmw3610_set_performance() not
      # being called before sleep") — last commit before the 2026-04-16 large
      # data-handling refactor (44b4a76). Battery-only hang has been reproduced
      # across all conductor-side configurations, suggesting the issue lives in
      # the dependency layer. Pinning here isolates the driver as a variable.
      revision: a1a0f35ab6750c8fc72b84ea00add1e5fbaea1e4
  self:
    path: config
